export type PoleEvent = {
  id: string;
  slug: string;
  title: string;
  flyer_url: string | null;
  starts_at: string;
  timezone: string;
  city: string;
  state: string;
  venue_name: string;
  category_label: string | null;
};
